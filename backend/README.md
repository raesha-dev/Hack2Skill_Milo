# Flask API for Milo App

- Deploy to Cloud Run with Google credentials and `OPENAI_API_KEY`, `GCS_BUCKET_NAME` as environment variables.
- Endpoints: `/api/chat`, `/api/sentiment`, `/api/mood`, `/api/tts`
